<!--Five are displaying-->
<!--movies needs title release year and description-->
<!--Clciking on it will bring it to  mvies page -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upcoming Movies</title>
    <link rel="stylesheet" href="/path/to/your/style.css"> 
</head>
<body>
    <%- include('partials/header') %> 
    <h1>Upcoming Movies</h1>
    <div class="movieContainer">
        <% movies.forEach(movie => { %>
            <div class="movieItem">
                <img src="<%= movie.image %>" alt="<%= movie.title %> Poster" class="poster">
                <h2><a href="/movie/<%= movie.id %>"><%= movie.title %></a></h2>
                <p><strong><span class="releaseYear">Release Year:</span></strong> <%= movie.releaseYear %></p>
                <p><strong><span class="descriptionLabel">Description:</span></strong> <%= movie.description %></p>
            </div>
        <% }); %>
    </div>

</body>
</html>
